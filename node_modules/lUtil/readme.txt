This folder(lUtil) is to save files used by in this project.
Even if this folder is saved in node_modules, this is not in remote repository.

#How to share this folder to git or bitbucket
We have to add "!node_modules/lUtil" and confirm gitignore_global and .gitignore file include not "node_modules" but "node_modules/*".
